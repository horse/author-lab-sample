# Runtime Run Records

<!-- 这是一个 sample，文件实质完成后删掉这行注释 -->

Store one immutable run record per condition and repetition. Every record must include commit SHA, runtime adapter version, model identifier, model parameters, context budget, tool permissions, runbook version, loaded file paths and hashes, timestamps, exit status, and output artifact references.
