# Runtime Adapter Contract

<!-- 这是一个 sample，文件实质完成后删掉这行注释 -->

Runtime adapters declare model identifiers, context limits, supported tools, environment variables, command entry points, and output locations. They must not contain author research or duplicate author-model content. A runtime may change without changing the identity of a derived author.