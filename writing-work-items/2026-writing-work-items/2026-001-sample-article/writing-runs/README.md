# Writing Runs

<!-- 这是一个 sample，文件实质完成后删掉这行注释 -->

Store one immutable `run-*.json` record per execution. Run outputs live under `writing-runs/<run-id>/`; never overwrite a prior run record or its output artifacts.
