# Writing Work-Item Instructions for Agents

<!-- 这是一个 sample，文件实质完成后删掉这行注释 -->

Read `work-item-state.json` first. Create new artifact versions instead of overwriting reviewed drafts. A work item may not enter `approved` or `published` unless factual and style reviews have passed and editor review is approved. Record every runtime run.